# 2026-07-12：WP-037 局后复盘数据

## 目标

把“我为什么输了、这一局做了什么”从模糊印象变成玩家和试玩观察员都能读懂的简短数据。它服务复盘，不做联网分析，也不悄悄记录个人信息。

## 完成内容

- `GameSession` 跨五个节点累计有效游玩时间、成功铺路、无效放置、实际受伤、实际治疗，以及加牌/护甲/移除次数。
- 暂停时间不进入耗时；绿色落点预览不算误操作；失败界面之后的点击不污染已经结束的统计。
- `reset()` 清空整局统计，`_start_node()` 保留统计，和生命、远征牌组的生命周期一致。
- 胜利与失败共用结算布局，显示两行紧凑复盘；同时修正旧文案 `All three nodes` 为当前的五节点。
- 增加规则测试与耐久不变量，覆盖暂停、放置、伤害、治疗、奖励、重置和跨节点累计。
- 405×720 胜/负截图均已人工查看；结算数据、重开按钮和底部手牌不重叠。截图等待由 8 帧提高到 16 帧，降低首帧背景未稳定的偶发问题。

## 验证

- Node 规格：15/15。
- Godot 规则：227 条断言。
- 自动耐久：250 次完整远征，37,253 条不变量检查。
- `builds/ui-won.png`、`builds/ui-lost.png`：405×720 视觉检查通过；构建截图不提交仓库。
- 提交 `383e92f` 触发 Actions 运行 `29189621691`；Ubuntu 规则、耐久、Linux 导出、成品 smoke 和 artifact 全部成功。

## 已知边界

- 当前数据只活在本局内，不写磁盘、不上传，也没有历史趋势。
- `MISPLAYS` 只表示规则拒绝的真实放置，不等于玩家“不够聪明”；试玩访谈仍要问当时为什么点那里。
- 自动化没有替代 WP-016/WP-026 真人试玩。下一步应让陌生玩家独立完成一局，并把数据与口头反馈放在一起看。
