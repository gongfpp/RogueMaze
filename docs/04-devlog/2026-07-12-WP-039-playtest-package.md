# 2026-07-12：WP-039 试玩交付包与 5 人执行协议

## 目标

把“游戏能导出”推进到“能安全、清楚地交给陌生玩家，并让不同主持人记录出可比较的证据”。准备材料不冒充真实试玩完成。

## 完成内容

- 新增 Windows 一键试玩打包：默认先跑全量测试和 Windows/Linux 导出，再创建带时间戳的 ZIP。
- ZIP 只放玩家需要的 GUI 程序、操作/隐私说明、完整 Build 身份和 SHA-256；打包后读取 ZIP 目录，缺任一文件就失败。
- Linux Actions artifact 从单个二进制扩为同样的四文件交付组，并在上传前执行 `sha256sum --check`。
- 建立 `docs/06-playtest`：玩家说明、主持流程、匿名观察表。明确首局不教解法、只用 P01 等编号、没有 5 份真实记录就不能关闭 WP-016/WP-026。
- PowerShell 5 会误解无 BOM 脚本中的部分 Unicode 字符，因此 BUILD 身份使用 ASCII `|`，不让漂亮分隔点变成乱码。

## 本地证据

- 打包前规则 15/15、工具 4/4、Godot 232 条断言、250 轮/37,253 条耐久检查通过。
- Windows 成品 smoke 身份为 `v0.4.0 / windows / 4618ac4`，法律声明与主场景标记通过。
- 首个 ZIP 约 43.5 MB，包含 4 个必需文件；`RogueMaze.exe` SHA-256 为 `f8ea4648e9c25bf71dbd00a85144176973d1ae0640ea883dda05a70a056ba859`。
- 该包由有未提交 WP-039 变更的工作树生成，因此正确标记 `*`，只用于验证流水线，不作为正式测试批次。

## 下一步

- 推送后由 Actions 验证 Linux 四文件 artifact。
- 下一份真正发给 5 位玩家的包应在干净提交上重新生成，不应带 dirty 星号。
- 用户已提供连接 USB 调试的 Android 手机；WP-025 接下来优先定位 ADB/SDK、构建 APK、安装启动和采集真机证据。
